import tkinter as tk

def parent():
    print(__name__)
    # root = tk.Tk()
    # top_frame = tk.Frame(root, bg='lavender', width=800, height=800, padx=3, pady=3)
    # root.grid_rowconfigure(1, weight=1)
    # root.grid_columnconfigure(0, weight=1)
    #
    # top_frame.grid(row=0, sticky ='news')
    # root.mainloop()
    # root.quit()
